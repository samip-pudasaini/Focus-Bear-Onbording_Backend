services:
  db:
    image: postgres:18
    container_name: postgres-dev5
    environment:
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5431:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: